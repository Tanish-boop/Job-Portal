<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register - Job Portal</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <style>
      :root {
        font-family: 'Inter', sans-serif;
      }
    </style>
  </head>
  <body class="h-screen bg-gray-100">
    <!-- Navigation (Matches Login Page) -->
    <nav class="bg-white shadow-md">
      <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <a class="text-xl font-bold text-gray-800 flex items-center" href="/">
          <i class="fas fa-briefcase text-indigo-600 mr-2"></i>
          JobPortal
        </a>
        <div class="text-sm">
          <a class="text-indigo-600 hover:text-indigo-800 font-medium" href="/login">
            Already have an account? Login
          </a>
        </div>
      </div>
    </nav>


    <main class="flex flex-col items-center justify-center flex-grow p-4 overflow-y-auto min-h-[calc(100vh-60px)]">
      <!-- Card Container: Max width is slightly larger for the extra fields -->
      <div 
        class="w-full max-w-md bg-white rounded-xl shadow-2xl overflow-hidden transform transition duration-500 hover:scale-[1.01]"
      >
        <div class="p-8">
          <div class="text-center mb-6">
            <i class="fas fa-user-plus fa-3x text-indigo-600 mb-3"></i>
            <h2 class="text-2xl font-extrabold text-gray-900">Create Account</h2>
            <p class="text-gray-500 text-sm mt-1">Join our community and start your journey</p>
          </div>
          
        
          <form action="/register" method="POST">
            <div class="mb-4">
              <label for="name" class="block text-sm font-medium text-gray-700 mb-1">
                <i class="fas fa-user mr-2"></i>Full Name / Company Name
              </label>
              <input
                type="text"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 transition duration-150"
                id="name"
                name="name"
                placeholder="Enter your full name or company name"
                required
              />
            </div>

            <div class="mb-4">
              <label for="email" class="block text-sm font-medium text-gray-700 mb-1">
                <i class="fas fa-envelope mr-2"></i>Email Address
              </label>
              <input
                type="email"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 transition duration-150"
                id="email"
                name="email"
                placeholder="Enter your email address"
                required
              />
            </div>

            <div class="mb-4">
              <label for="password" class="block text-sm font-medium text-gray-700 mb-1">
                <i class="fas fa-lock mr-2"></i>Password
              </label>
              <input
                type="password"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 transition duration-150"
                id="password"
                name="password"
                placeholder="Create a strong password"
                required
              />
              <div class="text-xs text-gray-500 mt-1">
                Password must be at least 6 characters long.
              </div>
            </div>

            <div class="mb-6">
              <label for="role" class="block text-sm font-medium text-gray-700 mb-1">
                <i class="fas fa-users mr-2"></i>Account Type
              </label>
              <select class="w-full px-4 py-2 border border-gray-300 bg-white rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 transition duration-150" id="role" name="role" required>
                <option value="">Choose your account type</option>
                <option value="seeker">
                  👤 Job Seeker - Looking for opportunities
                </option>
                <option value="recruiter">🏢 Recruiter - Hiring talent</option>
              </select>
            </div>

            <button 
              type="submit" 
              class="w-full flex justify-center items-center py-3 px-4 border border-transparent text-lg font-semibold rounded-lg shadow-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition duration-200 ease-in-out transform hover:scale-[1.005]"
            >
              <i class="fas fa-user-plus mr-2"></i>Create Account
            </button>
          </form>

          <div class="text-center mt-6">
            <p class="text-sm text-gray-600">
              Already have an account?
              <a href="/login" class="text-indigo-600 hover:text-indigo-800 font-bold transition duration-150">
                Sign in here
              </a>
            </p>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
